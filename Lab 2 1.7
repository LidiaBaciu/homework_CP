#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n;
    int OK=1;
    int i;

    printf("Numarul n este:");
    scanf("%d", &n);

    for(i=2; i<=n/2; i++)
        if(n%i==0)
            {
                OK=0;
                break;
            }

    if(OK==1)
        printf("Numarul este prim");
            else
                printf("Numarul nu este prim");

    return 0;
}
