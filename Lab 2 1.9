#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    char name[20];
    gets(name);

    char name_pirates[20]="PIRATES";
    char name_penguins[20]="PENGUINS";
    char name_steelers[20]="STEELERS";

    if(stricmp(name,name_pirates)==0)
        printf("Home run!");

        else
            if(stricmp(name,name_penguins)==0)
                printf("GOAL!");

                else
                    if(stricmp(name,name_steelers)==0)
                        printf("Touchdown!");

                        else printf("You lose");


    return 0;
}
