#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct mylist {

    int info;
    struct mylist *link;
} Node;

Node *_getnode ();


int main()
{

    Node *s = _getnode();
    s -> link = NULL;
    Node *t = s;

    int i;
    int sum=0;
    float average=0;


    for(i=0; i<25; i++){
            printf("\n\t Please enter the data for s info [%d]: ", i);
            scanf("%d", &s -> info);

            sum = sum + s->info;

            s -> link = _getnode();
            s = s -> link;



    }

    printf("Suma este: %d", sum);
    average = sum/25;
    putchar('\n');
    printf("Media este: %f", average);
    return 0;
}

Node *_getnode (){

    return ((Node *) malloc(sizeof (Node)));

}
