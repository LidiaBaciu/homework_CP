#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n;
    printf("Numarul n este:");
    scanf("%d", &n);
    putchar('\n');

    int cexp2=0;
    int cexp5=0;
    int p=1;
    int i;
    int x;

    for(i=1;i<=n;i++)
    {
        printf("numarul xi este:");
        scanf("%d", &x);

        if(x==0)
            p=0;
        else
        {
            while(x%2==0)
                {
                    cexp2++;
                    x=x/2;
                }

            while(x%5==0)
                {
                    cexp5++;
                    x=x/5;
                }
        }

    }

    if(p==0)
        printf("Produs nul");

        else
        {
            printf("Numarul de zerouri finale este:");
            if(cexp2>cexp5)
                printf("%d", cexp5);

                else
                    printf("%d", cexp2);
        }


    return 0;
}
