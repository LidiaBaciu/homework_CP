#include <stdio.h>
#include <stdlib.h>
#include <string.h>


int main()
{
    char string1[100], string2[100], string3[100];

    char *p, aux[100];

    printf("Sirul 1 este:\n");
    gets(string1);

    printf("Sirul 2 este:\n");
    gets(string2);

    printf("Sirul 3 este:\n");
    gets(string3);

    p=strstr(string1, string2);

    while( p != 0)
    {
        strcpy(aux, p+strlen(string2));
        strcpy(p, string3);
        strcpy(p+strlen(string3), aux);
        p=strstr(p+strlen(string3), string2);
    }

    printf("Sirul este: %s", string1);
    return 0;
}
