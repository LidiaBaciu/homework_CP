#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    int nr_cuvinte;
    printf("Numarul de cuvinte este:");
    scanf("%d", &nr_cuvinte);

    int i;
    int j;
    char string[20];
    for(i=1; i<=nr_cuvinte; i++)
        {
        printf("Cuvantul [%d] este:\n", i);

        gets(string);

        for(j=0; j< strlen(string); j++)
            {
                if(string[j]>='a' && string[j]<='z')
                printf("%c", string[j]-32);

                    else
                        printf("%c", string[j]);
            }
        }

    return 0;
}
