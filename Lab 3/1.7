#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n;

    do{
        printf("Numarul n este:");
        scanf("%d", &n);
        putchar('\n');
        if(n<0||n>90)
            {
                printf("Numar invalid");
                putchar('\n');
            }
                else
                    printf("Numar valid");
        }while(!(n>=0&&n<=90));

    return 0;
}
